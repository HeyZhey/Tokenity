# Portable, build-time defaults. Every value may be overridden in the
# environment before running a packaging script.
TOKENITY_INSTALL_ROOT_DEFAULT="/Library/Tokenity"
TOKENITY_RUNTIME_MEDIA_ROOT_DEFAULT="/Volumes"
